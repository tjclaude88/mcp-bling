# Compiled output — generated by `npm run build`, do not commit
dist/

# Node.js dependencies — installed locally, not committed
node_modules/

# Environment files — may contain secrets
.env
.env.*
.claude/

# Project instructions — local only, not committed
CLAUDE.md
